% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric-cram-index.R
\name{score_cram_index}
\alias{score_cram_index}
\title{Score CRAM Index}
\usage{
score_cram_index(
  cram,
  vegetativecover_data,
  function_name = c("Plant", "SLR"),
  indicator_name = "habitat",
  year = "all",
  cram_min = 25,
  cram_range = 75
)
}
\arguments{
\item{cram}{A data frame with columns \code{Site}, \code{Year_assessment},
and \code{index}.}

\item{vegetativecover_data}{A vegetation data frame with columns
\code{estuaryname}, \code{siteid}, and \code{samplecollectiondate}.}

\item{function_name}{Character vector. One or both of \code{"Plant"} and
\code{"SLR"}. One output block is produced per value. Default
\code{c("Plant", "SLR")}.}

\item{indicator_name}{Character. Indicator label. Default \code{"habitat"}.}

\item{year}{Numeric or character vector of years to include, or \code{"all"}.
Default \code{"all"}.}

\item{cram_min}{Numeric. Minimum possible CRAM score. Default 25.}

\item{cram_range}{Numeric. Divisor for the normalization formula. Default 75.}
}
\value{
A data frame with columns: estuaryname, siteid, year, function_name,
indicator_name, metric_name, metric_score.
}
\description{
For the selected year(s): filters vegetation data by \code{samplecollectiondate},
filters CRAM data by \code{Year_assessment}, averages the \code{index} column
per \code{Site} and year, then left-joins onto vegetation sites so every
surveyed site appears in the output (NA score when no CRAM data exists).
}
